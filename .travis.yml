language: php
php:
  - 7.0
  - 7.1
  - hhvm

sudo: required

install:
  - composer update --prefer-source --no-interaction --no-progress

script:
  - mkdir -p build/logs
  - phpunit

after_script:
 - php vendor/bin/coveralls -v
 
before_install:
  - pip install codecov
  
after_success:
  - codecov
